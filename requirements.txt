# Runtime dependencies. For an editable install use: pip install -e ".[dev]"
# (the [dev] extra adds pytest + ruff; [anthropic] adds the real backend).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
